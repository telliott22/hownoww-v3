export type Bond = {
    title: string,
    issuer: string,
    maturity: string,
    coupon: string,
    bidPrice: string,
    askPrice: string,
    bidPercentage: string,
    askPercentage: string,
    amountOutstanding: string
  }


export type NavItem = {
  name: string,
  href: string
}